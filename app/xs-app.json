{
    "welcomeFile": "/cp.portal",
    "routes": [
        {
            "source": "^/customers/(.*)$",
            "target": "$1",
            "authenticationType": "xsuaa",
            "destination": "srv_api"
        }
    ]
}